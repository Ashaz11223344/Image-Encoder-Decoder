# Image-Encoder-Decoder
A privacy-focused web tool that converts images into reversible noise patterns using a custom XOR algorithm. 100% client-side processing (no server uploads) with drag-and-drop, adjustable intensity, and dark mode.
